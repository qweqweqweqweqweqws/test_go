# Инструкции по деплою

## Быстрые варианты деплоя для тестирования

### 1. Render.com (РЕКОМЕНДУЕТСЯ для начала) ⭐

**Самое простое решение:**

1. Зарегистрируйтесь на https://render.com (бесплатно)
2. Создайте новый Web Service
3. Подключите ваш Git репозиторий (GitHub/GitLab/Bitbucket)
4. Настройки:
   - **Build Command**: `go build -o main main.go`
   - **Start Command**: `./main`
   - **Environment**: `Go`
   - **Environment Variables**:
     - `PORT` = `8080` (или оставьте пустым, Render сам назначит)
     - `ENVIRONMENT` = `production`

**Плюсы**: Бесплатный tier, автоматический деплой из Git, простой интерфейс

---

### 2. Railway.app

**Очень простой деплой:**

1. Зарегистрируйтесь на https://railway.app (есть бесплатный tier)
2. Создайте новый проект
3. Подключите Git репозиторий или загрузите код напрямую
4. Railway автоматически определит Go проект и соберет его
5. Добавьте переменные окружения в настройках:
   - `PORT` (Railway сам назначит, но можно указать)
   - `ENVIRONMENT` = `production`

**Плюсы**: Автоматическое определение языка, простой деплой

---

### 3. Fly.io

**Хорошо для production:**

1. Установите Fly CLI: `curl -L https://fly.io/install.sh | sh`
2. Зарегистрируйтесь: `fly auth signup`
3. В директории проекта запустите: `fly launch`
4. Следуйте инструкциям (Fly использует `fly.toml` из репозитория)
5. Деплой: `fly deploy`

**Плюсы**: Быстрый, глобальный CDN, хорошая производительность

---

### 4. Heroku

**Классический вариант:**

1. Установите Heroku CLI
2. Создайте файл `Procfile`:
```
web: ./main
```
3. Войдите: `heroku login`
4. Создайте приложение: `heroku create your-app-name`
5. Деплой: `git push heroku main`

**Примечание**: Heroku теперь платный, но есть бесплатный trial

---

### 5. Google Cloud Run (через Docker)

**Используя Dockerfile:**

1. Установите Google Cloud SDK
2. Соберите Docker образ: `gcloud builds submit --tag gcr.io/YOUR_PROJECT_ID/back`
3. Деплой: `gcloud run deploy back --image gcr.io/YOUR_PROJECT_ID/back --platform managed`

---

### 6. DigitalOcean App Platform

1. Зарегистрируйтесь на DigitalOcean
2. Создайте новый App
3. Подключите Git репозиторий
4. Выберите тип: Docker (или Go, если поддерживается)
5. Настройте переменные окружения

---

## Быстрый старт с Render.com (рекомендуется)

1. **Подготовьте репозиторий**: Загрузите код на GitHub/GitLab
2. **Создайте аккаунт**: https://render.com
3. **Новый Web Service** → подключите репозиторий
4. **Настройки**:
   ```
   Build Command: go build -o main main.go
   Start Command: ./main
   ```
5. **Environment Variables**:
   ```
   PORT=8080
   ENVIRONMENT=production
   ```
6. **Deploy** → Готово! 🚀

---

## Переменные окружения для production

Убедитесь, что установлены:
- `PORT` - порт (обычно платформа сама назначает)
- `ENVIRONMENT=production` - режим работы

---

## Тестирование после деплоя

После деплоя вы получите URL вида: `https://your-app.onrender.com`

Проверьте:
```bash
curl https://your-app.onrender.com/api/v1/health
```

---

## Примечания

- Для cross-origin запросов убедитесь, что используете HTTPS (все платформы выше предоставляют его)
- CORS middleware настроен для работы с любыми доменами
- Cookie будут работать только по HTTPS в production (SameSite=None требует Secure=true)

